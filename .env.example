# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase (project: brick media / unstuck schema)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripe (test mode)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PLUS_MONTHLY=price_...

# Optional: OpenAI for agent responses (otherwise heuristic fallback)
OPENAI_API_KEY=

# Local dev only: skip /app login redirect and attempt anonymous Supabase sign-in
# (requires Authentication → Providers → Anonymous enabled in Supabase)
# Never set in production.
AUTH_BYPASS=true
